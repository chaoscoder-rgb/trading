# Minimal runtime dependencies — exactly what the app imports.
# (The previous file was a full environment dump incl. a yanked llvmlite beta
# that broke Docker builds. ML libs get added back when features need them.)
fastapi==0.124.4
uvicorn==0.38.0
httpx==0.28.1
libsql-client==0.3.1
pydantic==2.12.5
pydantic-settings==2.12.0
cryptography==46.0.3
python-dotenv==1.2.1
